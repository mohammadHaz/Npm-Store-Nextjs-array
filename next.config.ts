import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
   reactStrictMode: false,
  //  output: 'export', // مهم لتحويل المشروع إلى static site

};

export default nextConfig;
